<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= itemNamePlural %> - Railway Model Inventory</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('../partials/header.ejs') %>

  <main class="container">
    <div class="page-header">
      <h1 class="page-title">🛤️ <%= itemNameSingular %> Collection</h1>
      <p class="page-subtitle">Explore our premium selection of model <%= itemNamePlural.toLowerCase() %></p>
      <div style="margin-top: 1.5rem;">
        <a class="btn btn-primary" href="/<%= path %>/add">Add <%= itemNameSingular %></a>
      </div>
    </div>

    <%- include('../partials/filters-container.ejs', { path }) %>
    
    <% if (items && items.length > 0) { %>
      <div class="product-grid">
        <% items.forEach(item => { %>
          <%- include('../partials/item-card.ejs', { item }) %>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <div class="empty-state-icon">🛤️</div>
        <h2 class="empty-state-title">No <%= itemNamePlural %> Found</h2>
        <p class="empty-state-message">We couldn't find any <%= itemNamePlural.toLowerCase() %> in our inventory. Check back soon!</p>
        <a href="/" class="btn btn-secondary">Return Home</a>
      </div>
    <% } %>
  </main>

  <%- include('../partials/footer.ejs') %>
</body>
</html>